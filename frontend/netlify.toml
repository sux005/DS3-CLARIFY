[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  # After the Render backend is live, add VITE_API_URL in:
  # Netlify UI → Site configuration → Environment variables
