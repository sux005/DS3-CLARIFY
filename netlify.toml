[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"
  # Set VITE_API_URL in Netlify UI → Site settings → Environment variables
